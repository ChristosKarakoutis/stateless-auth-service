# JWT key paths — defaults to classpath:keys/private.pem and classpath:keys/public.pem
# Override with absolute paths or Docker-mounted files if needed.
# JWT_PRIVATE_KEY=/run/secrets/private.pem
# JWT_PUBLIC_KEY=/run/secrets/public.pem
